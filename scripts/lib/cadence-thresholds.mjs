/**
 * Пороги AI-каденции для португальского сайта (английский корпус).
 *
 * Движок `scripts/lib/ai-cadence.mjs` и CLI `scripts/cadence-check.mjs` общие с
 * moregroupestate-ru и more-group-website, байт в байт. Разница между сайтами
 * живёт только здесь: язык шаблонов и потолки по коллекциям. Расхождение копий
 * движка ловит `npm run cadence:parity`.
 *
 * КАК МЕНЯТЬ ПОРОГИ. Числа посчитаны командой `node scripts/cadence-check.mjs
 * --calibrate --json` по живому корпусу этого сайта одним правилом для всех
 * сайтов, без подбора на глаз:
 *   maxPer1k        = p90 плотности, округлённый до ближайших 0.5, но не ниже 2.5;
 *   maxMetronomePct = p90 метронома, округлённый вверх до кратного 5, в пределах 20..60;
 *   minCv           = p10 CV минус 0.05, округлённый вниз до 0.05, в пределах 0.25..0.35.
 * Квантили линейные, как в самом --calibrate. Порог из головы здесь запрещён:
 * гейт по GEO-баллу, выставленный до замера, кончился тем, что машинные тексты
 * набирали 92, а рукописные 78.
 */

export const LANG = 'en';

/** Замер 22.09.2026, 178 файлов длиннее 250 слов, ветка cc/cadence-port. */
export const CALIBRATION = {
  measuredAt: '2026-09-22',
  files: 178,
  corpus: {
    per1k: { p50: 1.99, p75: 2.76, p90: 3.88, max: 6.01 },
    metronomePct: { p50: 15, p75: 23, p90: 27, max: 51 },
    cv: { p10: 0.47, p25: 0.50, p50: 0.61 },
  },
  /**
   * Сумма попаданий по категориям на весь корпус: antithesis 878, colonHook 43,
   * superlativeOpener 38, headingHook 3, calque 1, clerical 0, literary 0,
   * impersonal 0. Итого 963, из них антитеза 91% (4.93 на файл).
   *
   * Вывод тот же, что на Пхукете: почти весь почерк это ОДНА фигура речи,
   * антитеза («X, not Y», «not X but Y»). Канцелярит и пафос, которые ловил
   * прежний гейт (BANNED_PHRASES и AI_FLUFF_RE в lib/more-content-gate.mjs),
   * дают на весь корпус 0 попаданий: прежняя проверка сторожила то, чего в
   * корпусе уже нет, и не видела того, что есть.
   *
   * Невидимых символов в корпусе нет ни одного, неразрывных пробелов тоже.
   */
  categoryTotals: {
    antithesis: 878,
    colonHook: 43,
    superlativeOpener: 38,
    headingHook: 3,
    calque: 1,
    clerical: 0,
    literary: 0,
    impersonal: 0,
  },
  dominantCategory: 'antithesis',
};

export const CADENCE_THRESHOLDS = {
  // p50 1.99 / p75 2.76 / p90 3.88: p90 до ближайших 0.5 даёт 4.0
  maxPer1k: 4.0,
  // p50 15 / p75 23 / p90 27.2: вверх до кратного 5 даёт 30
  maxMetronomePct: 30,
  // p10 0.47 / p25 0.50 / p50 0.61: 0.47 - 0.05 = 0.42, вниз до 0.40, потолок правила 0.35
  minCv: 0.35,
  maxParallel: 3,
  // Повтор n-граммы шумит на шаблонных карточках: только отчёт.
  maxRepeated: null,
  // Ниже 300 слов плотность на 1000 слов перестаёт быть устойчивой: четыре
  // попадания на 264 слова дают 15/1k и выглядят как провал на ровном месте.
  minWords: 300,
};

/**
 * Профили по коллекциям, каждый по тем же формулам на данных раздела.
 * Профиль получает только раздел, где не меньше 8 файлов длиннее порога слов.
 * Без профиля (идут по базе): projects (7 файлов), developers (3 файла),
 * news (пусто).
 */
export const COLLECTION_PROFILES = {
  // n=80. Плотность p75 3.36 / p90 4.31 -> 4.5; метроном p75 23 / p90 27.8 -> 30;
  // CV p10 0.48 -> 0.43 -> 0.40 -> потолок 0.35.
  // Плотность ослаблена с 4.5 до 5.5 по правилу доли помеченных: по формулам
  // помечалось 50 из 178 файлов (28.1%) при потолке 25%, больше всего в guides
  // (19). Шаг 5.0 дал 45 из 178 (25.3%), всё ещё выше; шаг 5.5 дал 44 из 178
  // (24.7%). Остальные пометки guides это метроном и параллельные старты,
  // плотностью их не снять.
  guides: { maxPer1k: 5.5, maxMetronomePct: 30, minCv: 0.35 },
  // n=26. Плотность p75 1.87 / p90 2.40 -> 2.5; метроном p75 14 / p90 23.3 -> 25;
  // CV p10 0.46 -> 0.41 -> 0.40 -> потолок 0.35
  areas: { maxPer1k: 2.5, maxMetronomePct: 25, minCv: 0.35 },
  // n=23, карточный раздел: minWords 400 и maxParallel 4.
  // Плотность p75 2.08 / p90 2.60 -> 2.5; метроном p75 25.6 / p90 27.0 -> 30;
  // CV p10 0.75 -> 0.70 -> потолок 0.35
  'property-for-sale': { maxPer1k: 2.5, maxMetronomePct: 30, minCv: 0.35, minWords: 400, maxParallel: 4 },
  // n=15. Плотность p75 2.34 / p90 2.74 -> 2.5; метроном p75 14.6 / p90 21.4 -> 25;
  // CV p10 0.41 -> 0.36 -> 0.35
  compare: { maxPer1k: 2.5, maxMetronomePct: 25, minCv: 0.35 },
  // n=13. Плотность p75 2.68 / p90 3.52 -> 3.5; метроном p75 14.0 / p90 14.7 -> 15,
  // нижняя граница правила 20; CV p10 0.50 -> 0.45 -> потолок 0.35
  segments: { maxPer1k: 3.5, maxMetronomePct: 20, minCv: 0.35 },
  // n=11. Плотность p75 3.10 / p90 3.18 -> 3.0; метроном p75 27.6 / p90 30 -> 30;
  // CV p10 0.67 -> 0.62 -> потолок 0.35
  'move-to-portugal': { maxPer1k: 3.0, maxMetronomePct: 30, minCv: 0.35 },
};

export function cadenceProfileFor(collection) {
  return { ...CADENCE_THRESHOLDS, ...(COLLECTION_PROFILES[collection] || {}) };
}
